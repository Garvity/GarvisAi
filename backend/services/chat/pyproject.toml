[project]
name = "chat"
version = "1.0.0"
description = "Chat service: conversations and messages in MongoDB"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "beanie>=1.29,<2.0",
    "python-dotenv>=1.0",
]
